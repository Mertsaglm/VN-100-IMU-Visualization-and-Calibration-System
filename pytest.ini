[pytest]
# Restricts collection to tests/ so pytest doesn't pick up unrelated files in the repo root.
# tools/verify.py invokes this same pytest, keeping single-command verification reproducible.
testpaths = tests
